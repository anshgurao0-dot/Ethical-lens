version: '3.8'

services:
  backend:
    build: ./backend-agents
    ports:
      - "8000:8000"
    volumes:
      - ./backend-agents:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always

  # Future: Add Database (Postgres)
  # db:
  #   image: postgres:13
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: ethical_lens_db
